package vector;

public enum Dimensions {
	
	TWODIM(i, j),
	THREEDIM(i, j, k);
	
	private Vector i = null;
	private Vector j = null;
	private Vector k = null;
	
	private Dimensions(Vector... unitVecs) {
		if (unitVecs.length == 2) {
			i = unitVecs[0];
			j = unitVecs[1];
			k = null;
		} else if (unitVecs.length == 3) {
			i = unitVecs[0];
			j = unitVecs[1];
			k = unitVecs[2];
		}
	}
}
