package vector;

public enum Dimensions {
	
	ONEDIM(1),
	TWODIM(2),
	THREEDIM(3);
	
	private Vector i = null;
	private Vector j = null;
	private Vector k = null;
	private int numDimensions;
	private VectorFactory vf = null;
	
	private Dimensions(int numDims) {
		numDimensions = numDims;
		if (numDimensions == 1) {
			vf = new VectorFactory(numDims);
			i = vf.getI();
			j = null;
			k = null;
		}else if (numDimensions == 2) {
			vf = new VectorFactory(TWODIM);
			i = vf.getI();
			j = vf.getJ();
			k = null;
		} else if (numDimensions >= 3) {
			vf = new VectorFactory(THREEDIM);
			i = vf.getI();
			j = vf.getJ();
			k = vf.getK();
		} else {
			i = null;
			j = null;
			k = null;
		}
	}
}
